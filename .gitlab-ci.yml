# GitLab CI 配置 - 可选
# 如果需要自动化构建和测试，可以使用这个文件

stages:
  - build

build:
  stage: build
  image: node:18
  script:
    - npm install
    - npm run build
  artifacts:
    paths:
      - dist/
    expire_in: 1 week
  only:
    - main
    - develop

